# AUTO-GENERATED; DO NOT MODIFY;
# This file is auto-generated by kubara and should not be modified manually in any way.
# To customize values, you can add additional files named values-*.yaml to this directory.

namespace:
  labels:
    project-name: "spoke-agent-test"
    stage: "dev"

externalSecrets:

  secretStoreRef:
    kind: ClusterSecretStore
    name: spoke-agent-test-dev


  secrets:
    oauth2-credentials:
      dataFrom:
        - remoteKey: spoke-agent-test/dev/oauth2-proxy/oauth2_credentials


oauth2-proxy:
  imagePullSecrets:
    - name: image-pull-secret
  config:
    existingSecret: oauth2-credentials
    configFile: |-
      reverse_proxy = true
      redirect_url = "https://spoke-agent.local/oauth2/callback"
      whitelist_domains = [ "spoke-agent.local" ]
      email_domains = [ "*" ]
      cookie_secure = true
      upstreams = [ "static://200" ]
      github_org = "local"
      github_team = "local"
      provider = "github"
      scope = "read:user user:email read:org"
  ingress:
    className: traefik
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-staging
    enabled: true
    path: /oauth2
    pathType: Prefix
    hosts:
      - spoke-agent.local
    tls:
      - secretName: tls-oauth2-proxy
        hosts:
          - spoke-agent.local
  metrics:
    serviceMonitor:
      enabled: true
      labels:
        monitoring.instance: "spoke-agent-test-dev"


middleware:
  name: oauth-auth
  namespace: oauth2-proxy

  forwardAuth:
    address: http://oauth2-proxy.oauth2-proxy.svc.cluster.local/oauth2/
    trustForwardHeader: true

    authResponseHeaders:
      - X-Auth-Request-User
      - X-Auth-Request-Email
      - Authorization
