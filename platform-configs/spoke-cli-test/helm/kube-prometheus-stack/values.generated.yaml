# AUTO-GENERATED; DO NOT MODIFY;
# This file is auto-generated by kubara and should not be modified manually in any way.
# To customize values, you can add additional files named values-*.yaml to this directory.

kube-prometheus-stack:
  global:
    imagePullSecrets:
      - name: image-pull-secret
    imageRegistry: ""
  prometheusOperator:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: spoke-cli-test-local
    prometheus:
      monitor:
        additionalLabels:
          monitoring.instance: spoke-cli-test-local
  prometheus:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: spoke-cli-test-local
    ingress:
      ingressClassName: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-staging
        traefik.ingress.kubernetes.io/router.middlewares: oauth2-proxy-oauth-auth@kubernetescrd
      enabled: true
      paths:
        - /prometheus
      pathType: ImplementationSpecific
      hosts:
        - spoke.local
      tls:
        - secretName: tls-prometheus
          hosts:
            - spoke.local


    prometheusSpec:
      retention: 10d
      serviceMonitorSelector:
        matchLabels:
          monitoring.instance: spoke-cli-test-local
      storageSpec:
        volumeClaimTemplate:
          spec:
            accessModes:
              - ReadWriteOnce
            resources:
              requests:
                storage: 10Gi
      externalUrl: https://spoke.local/prometheus
      routePrefix: /prometheus
    thanosServiceMonitor:
      serviceMonitor:
        additionalLabels:
          monitoring.instance: spoke-cli-test-local
  alertmanager:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: spoke-cli-test-local
    ingress:
      ingressClassName: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-staging
        traefik.ingress.kubernetes.io/router.middlewares: oauth2-proxy-oauth-auth@kubernetescrd
      enabled: true
      paths:
        - /alertmanager
      pathType: ImplementationSpecific
      hosts:
        - spoke.local
      tls:
        - secretName: tls-alertmanager
          hosts:
            - spoke.local


# Below is an example alertmanager.config section:
# It demonstrates an advanced routing setup that includes Microsoft Teams (v2), a watchdog receiver
# (e.g. for UptimeRobot) and a silenced route for informational alerts.
# This config use webhook urls from secret-files (see alertmanagerSpec below)
#
#    config:
#      route:
#        receiver: msteams-notifications  # Default receiver if no route matches
#        routes:
#          # 1. Silence all alerts named "InfoInhibitor"
#          - receiver: "null"
#            matchers:
#              - alertname =~ "InfoInhibitor"
#          # 2. Route "Watchdog" alerts to UptimeRobot with a short repeat interval
#          - receiver: uptimerobot
#            repeat_interval: 1m
#            matchers:
#              - alertname =~ "Watchdog"
#          # 3. Route all alerts with severity "warning" or "critical" to Microsoft Teams
#          - receiver: 'msteams-notifications'
#            matchers:
#              - severity =~ "warning|critical"
#            continue: true  # Continue to evaluate other routes after this one
#      receivers:
#        # Microsoft Teams v2 receiver using `msteamsv2_configs`
#        - name: 'msteams-notifications'
#          msteamsv2_configs:
#            - send_resolved: true
#              webhook_url_file: /etc/alertmanager/secrets/msteams-webhook-url
#        # Watchdog/Uptime monitoring receiver
#        - name: uptimerobot
#          webhook_configs:
#            - url_file: /etc/alertmanager/secrets/uptimerobot-webhook-url
#        # Null receiver to discard matched alerts
#        - name: "null"
#      # Optional: Use custom templates for consistent alert formatting
#      templates:
#        - /etc/alertmanager/config/*.tmpl

    alertmanagerSpec:
#      # secrets for alertmanager config webhook urls
#      secrets:
#        - msteams-webhook-url
#        - uptimerobot-webhook-url

      externalUrl: https://spoke.local/alertmanager
      routePrefix: /alertmanager

  kube-state-metrics:
    prometheus:
      monitor:
        additionalLabels:
          monitoring.instance: spoke-cli-test-local
  kubeApiServer:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: spoke-cli-test-local
  kubelet:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: spoke-cli-test-local
  kubeControllerManager:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: spoke-cli-test-local
  coreDns:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: spoke-cli-test-local
  kubeDns:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: spoke-cli-test-local
  kubeScheduler:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: spoke-cli-test-local
  thanosRuler:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: spoke-cli-test-local
  grafana:
    admin:
      existingSecret: grafana-admin-credentials
      userKey: admin-user
      passwordKey: admin-password
    serviceMonitor:
      labels:
        monitoring.instance: spoke-cli-test-local
    ingress:
      ingressClassName: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-staging
        traefik.ingress.kubernetes.io/router.middlewares: oauth2-proxy-oauth-auth@kubernetescrd
      enabled: true
      path: /grafana
      pathType: ImplementationSpecific
      hosts:
        - spoke.local
      tls:
        - secretName: tls-grafana
          hosts:
            - spoke.local
    envFromSecrets:
      - name: oauth2-credentials
    grafana.ini:
      server:
        root_url: https://spoke.local/grafana/
        serve_from_sub_path: true
      auth.github:
        name: "Github"
        enabled: true
        client_id: ${client-id}
        client_secret: ${client-secret}
        allow_sign_up: true
        auto_login: false
        scopes: "user:email,read:org"
        allowed_organizations: "[\"local\"]"
        role_attribute_path: contains(groups[*], '@local/local') && 'Admin' || 'Viewer'
    additionalDataSources:
      - access: proxy
        isDefault: false
        name: loki
        type: loki
        url: http://loki-headless.loki.svc.cluster.local:3100
        version: 1
prometheus-blackbox-exporter:
  image:
    pullSecrets:
      - image-pull-secret
  global:
    imageRegistry: quay.io

externalSecrets:

  secretStoreRef:
    kind: ClusterSecretStore
    name: spoke-cli-test-local


  secrets:
    grafana-admin-credentials:
      dataFrom:
        - remoteKey: spoke-cli-test/local/kube-prometheus-stack/grafana_credentials
    oauth2-credentials:
      dataFrom:
        - remoteKey: spoke-cli-test/local/kube-prometheus-stack/grafana_oauth2_credentials

namespace:
  labels:
    project-name: "spoke-cli-test"
    stage: "local"
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/warn: privileged
